# TODO
- [x] Update `instainsight/app/_layout.tsx` to render the navigation stack only on the client (client-only guard) to eliminate the `useLayoutEffect` SSR warning.

- [x] Re-run the web/server build to confirm the warning is gone.


