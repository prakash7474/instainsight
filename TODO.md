# TODO

- [ ] Replace `instainsight/components/StoryModal.tsx` with requested FlatList-based modal and inline `StoryViewer`.
- [ ] Update `instainsight/app/stories.tsx` to pass `stories` in required shape: `{ uri, type }`.
- [ ] Update story click flow in `instainsight/app/stories.tsx` to set `selectedIndex` + open modal.
- [ ] Add `console.log(stories)` in `instainsight/app/stories.tsx` before rendering.
- [x] Add `expo-av` plugin to `instainsight/app.json`.

- [ ] Run quick build/check (optional): `npx tsc -p instainsight/tsconfig.json` and/or `npx expo start -c` (optional).

